<%= render partial: 'shared/card_layout', locals: { collection: @user_lists, list: @list, item_type: 'list', collection_name: 'My Lists', show_create: true } %>
<%= render partial: 'shared/card_layout', locals: { collection: @lists, item_type: 'list', collection_name: 'Other Lists', show_create: false } %>
